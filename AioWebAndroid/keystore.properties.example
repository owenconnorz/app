# Local release-signing config — copy this file to `keystore.properties` and fill in real values.
# DO NOT commit keystore.properties — it's already in .gitignore.
storeFile=release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=aioweb
keyPassword=YOUR_KEY_PASSWORD
