<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Plain framework theme. The app uses Jetpack Compose Material 3 entirely, so
         we don't need com.google.android.material XML themes here.
         Using android:Theme.Material.NoActionBar avoids dependency on Material lib styles
         while still giving us a no-action-bar Activity host. -->
    <style name="Theme.AioWeb" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowTranslucentStatus">false</item>
    </style>

    <style name="Theme.AioWeb.Splash" parent="Theme.AioWeb">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
</resources>
